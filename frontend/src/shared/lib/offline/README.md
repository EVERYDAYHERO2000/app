# Offline queue

Здесь разместить логику очереди офлайн-действий (IndexedDB), синхронизацию при восстановлении сети и обработку конфликтов (CONFLICT + актуальный статус).

Детали сценариев офлайн/синхронизации — по внутренней спецификации проекта (локально).
